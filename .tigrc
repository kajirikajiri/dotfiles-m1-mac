# commit時にstagedファイルを表示(commitizenに貼り付けて使う)
bind status  C  !sh -c 'res=$(git diff --staged --name-only) && echo "${GREEN}${res}${NC}" && git cz'

# soft reset
bind generic ^  ?git reset --soft HEAD^
bind generic <Ctrl-f> scroll-page-down
bind generic <Ctrl-b> scroll-page-up

